# 个人账单管理系统 · 设计文档

> 状态:讨论稿,随讨论持续更新
> 工作目录:`/Users/apple/Documents/账单管理`(代码)
> 数据参考:`/Users/apple/Documents/个人账单分析`(只读)
> 主平台:Windows 桌面;开发环境:macOS;经 GitHub 流转

---

## 0. 定位

离线单机个人账本。**主视角 = 记账周期**,每天记账,周期结束自动出报表,支持平台账单导入(早期由 AI agent 辅助),SQLite 单文件存储 + SQLCipher 加密。不依赖浏览器 / 本地服务 / 外网。

## 1. 核心概念(已定)

- **记账周期 = 生活费周期**
  - 学期中:每月发生活费日起,到下次发生活费前一天止(例:5月3日 → 6月2日)
  - 假期:整个假期为一个周期
  - **不设周期类型枚举**(兼容未来可能没有假期)——用**备注**体现(预设:暑假 / 学期…可自定义)
- **周期生命周期**
  - 创建(向导)→ 进行中(active)→ 到期自动**推荐**新建下周期 → 用户**封存**(sealed)才算真正结束 → 只读 + 出报表
  - 上一周期在封存前**一直可修改**
  - 特殊时期可**手动新建**周期
- **记账方式**:每天一计("今天"卡片,往里加多笔)
- **主界面**:周期视图(收入/支出/结余/消耗速度)+ 侧边自然月月历(每天显示纯支出与收入)
- **报表**:按周期或选定日期范围;周期结束自动出,可选保存;也可主动申请
- **报表产物**:PDF(阅读/留档)+ xlsx(数据),不留内部快照

## 2. 技术栈(已定)

- C# / .NET 8 + **WinForms**(Windows 桌面;mac 上写、Windows 上编译)
- **SQLite + SQLCipher 全库加密**(打开需密码;`Microsoft.Data.Sqlite.Core` + `SQLitePCLRaw.bundle_e_sqlcipher`,原生 e_sqlcipher 由 NuGet 按 RID(win-x64)提供,自包含发布自动带上,无需手动拷 dll)
- **图表**:ScottPlot;报表:QuestPDF(PDF)+ ClosedXML(xlsx),ScottPlot 导出图片嵌入 PDF
- 流程:macOS 开发 → git push → Windows `dotnet build` / `dotnet publish -r win-x64`

## 3. 数据模型

### 3.1 periods —— 记账周期
```sql
periods
  id, name("2026春·6月" / "2026暑假")
  note            -- 备注(预设:暑假/学期…可自定义),不设硬编码类型
  start_date, end_date   -- 创建后结束日随时可手动改
  status          -- active / ended(已到期未封存) / sealed(已封存,只读)
```
封存不产生结转记录:**结余默认留在资金池目标账户**(现金自然延续);如需挪走,封存前手动记一笔转账。

### 3.2 accounts —— 账户
```sql
accounts
  id, name, platform(微信/支付宝/银行/投资/现金)
  type            -- wallet(零钱) / money_fund(零钱通余额宝) / bank / cash
                  -- fixed_deposit(整存整取) / fund(基金) / prepaid(公交卡/水卡储值卡)
  enabled         -- 是否纳入净资产
  balance_base, balance_date   -- 基准余额(启动/校准用)
```
账户清单:微信零钱、零钱通、支付宝余额、余额宝、建行储蓄卡、整存整取、基金、现金、水卡(prepaid)。公交卡不建账户(充值即支出)。(父母托管已移除)

**校准余额(对准实际):**
- 账面余额 = balance_base + Σ(账户相关流水)。零钱通/余额宝日结利息在平台账单不可见、提现费、漏记 → 账面会漂移,靠校准拉回
- 校准对话框:当前账面 / 实际余额 / 差额,三种处理方式:
  1. **记调整流水(推荐)**:差额≠0 生成一条流水,方向自动(实际>账面→收入,实际<账面→支出),分类固定「差额调整」(挂"其他"下,不计用途占比),`source=calibration`, `in_pool=false`;不变式始终成立
  2. **补记真实明细**:差额拆成真实交易补记,不留调整痕迹
  3. **仅更新基准**:不写流水直接改 balance_base(历史账对不上/不想留调整痕迹时用)
- **校准历史(审计)**:每次校准记 时间/账面/实际/差额/处理方式/备注,账户详情可查

### 3.3 categories —— 分类
```sql
categories
  id, parent_id, name, keyword(导入自动归类用)
  color        -- 主题色(所有图表统一引用;默认从固定调色板按序自动分配,可手动改)
  sort_order   -- 显示/堆叠顺序(分类管理中可拖排)
```
- **大类(用途)**:餐饮 / 交通 / 购物 / 教育 / 娱乐 / 医疗 / 居住 / 其他
- **小类 = 备注**,自由写,不设限
- **渠道 = 下拉字段**:网购 / 实体 / 其他(可留空),不参与用途占比
- **订阅按用途**:网易云→娱乐,iCloud→其他
- **用水**:记录界面保留独立入口,存储/报表合并进"居住"
- 收入分类:生活费 / 家人转账 / 红包 / 理财收益 / 其他

### 3.4 transactions —— 单表流水(每笔一条)
```sql
transactions
  id, period_id(由日期自动归属)
  date, account_id, to_account_id(仅 transfer 用)
  category_id, channel(渠道下拉)
  name           -- 名称(交易名,如"早餐"/"iCloud"),界面必填
  note           -- 备注(小类/细节,自由写)
  amount_cents   -- 整数"分";界面以"元"输入,Decimal 两位小数
  direction      -- in / out / transfer
  counterparty, source(manual/wx/zfb/legacy), ref_id(平台单号)
  status         -- normal / refunded / cancelled
  in_pool        -- 计入资金池:支出默认true(可勾"不入池");转账出池默认false(可勾"计入池");其余情况无效
  refund_of      -- FK→原交易(退款冲减用)
  group_parent   -- FK→混合支付中金额最大的一笔,其余拆分明细指向它
  transfer_kind  -- 转账类别(互转/充值/提现/理财结算/存取);独立于收支分类,避免污染手动录入列表
  created_at
```

### 3.5 transfer —— 本金 + 浮动值 + 分类
```sql
-- 方向=transfer 时:
--   from=account_id, to=to_account_id
--   转出账户 −principal;转入账户 +(principal + delta)
--   delta>0 → 记理财收益(收入);delta<0 → 记手续费(支出)
  principal_cents, delta_cents(默认0)
  category(互转/充值/提现/理财结算/存取)  -- 存 transfer_kind 列(见 §3.4)
```
示例:整存整取到期 10000→10062.5:`principal=10000, delta=+62.5, 分类=理财结算`;提现手续费 `delta=−0.1, 分类=提现`。

### 3.6 fund_pools —— 资金池(预算信封)
```sql
fund_pools
  id, period_id, name("生活费池")
  account_id      -- 单一账户为准,池外账户不计入
  budget_cents    -- 资金池大小(默认取初始收入金额;无初始收入可取目标账户余额)
  reserve_cents   -- 预计保留金额(创建时默认=大小×30%,可改)
reserve_items     -- 未来支出预留清单
  pool_id, due, item, amount_cents
```
**资金池机制(粒度,见 §4):**
- **进池只此一次**:仅初始划分时进,之后一切收入(生活费/工资/红包/利息)都不进池
- 已花 = 池账户本周期**直接支出且 in_pool=true** 合计 − 对应退款(退款恢复池)+ 勾选"计入池"的转出
- **支出默认入池**,可勾"不入池"(in_pool=false)排除特殊支出(代付/报销/大额给家里等)
- **转账出池账户**:可勾选"计入池"(默认不计);勾选后该转账从池剩余中扣除。池外账户收支一律不影响池
- 剩余 = 预算 − 已花(只减不增,除退款恢复、勾选的转出);可自由支配 = 剩余 − 未来预留
- 提醒:可自由支配 < 保留金额;剩余 < 0;预留项到期日临近
- **池剩余(预算口径)与账户余额(现金口径)是两个数,不要求相等**
- **多池**:设计保留,一期只做单一池
- **池可中途补建/修改**:创建周期时未建池,之后可在周期总览"设置资金池"补建;已花由账目派生,补建后自动正确,不会漏算

### 3.7 储值卡(公交卡 / 水卡)

预充值卡分两种记账方式,按卡选用:

- **模型一 · 充值即支出(公交卡)**:充值 = 一笔支出(分类=交通,账户=支付方,默认入池)。**不建卡账户、不追余额**,充值当天记完即止。
- **模型二 · 充值=转账 + 用卡支出(水卡)**:充值 = 转账(支付方 → 水卡,转出池账户可勾"计入池");每次用卡 = 支出(分类=居住/用水,账户=水卡)。水卡为 prepaid 账户,**计入净资产**,余额走校准通道(从卡 App 对账)。
- **池关系**:两种模型都在**充值那一刻占用池额度**(公交卡是支出天然入池;水卡充值勾"计入池");之后的用卡支出发生在非池账户,不影响池。

## 4. 资金池机制(粒度对齐)

> 一句话:资金池 = 周期内绑在"一个账户"上的预算信封,回答"这周期打算花的钱还剩多少额度"(不限于生活费,工作后可为工资)。

1. **绑定**:一个池绑一个账户(默认=生活费入账账户)。
2. **进池(只此一次)**:只在周期创建时初始划分一次,之后**一切收入都不进池**——生活费、工资、红包、利息一律不进。
3. **预算划分 vs 初始收入(分工明确)**
   - **划分池大小不看钱是什么**:生活费、工资、从账户余额划,都行。默认建议 = 初始收入金额;无初始收入可从目标账户余额取。
   - **初始收入是唯一被"定性"的钱**:它是流水表里带分类(生活费 / 工资 / 其他)的收入记录;它只作为池预算的默认建议来源,池划分独立于收入性质。
4. **已花**:池账户本周期直接支出(out)合计 − 对应退款(退款恢复池——退款不是新收入,是退回原支出)+ 用户勾选"计入池"的转出转账。
5. **剩余** = 预算 − 已花。只减不增,除退款恢复、勾选的转出。
6. **可自由支配** = 剩余 − 未来预留(reserve_items)。
7. **提醒**:可自由支配 < 保留金额 / 剩余 < 0 / 预留到期。
8. **封存**:周期结余(全账户收入−支出)默认留在池账户;用户可封存前手动转账挪走。封存后只读 + 出报表。

## 5. 周期创建引导程序(向导)

1. **日期范围**:开始日(默认今天/上次周期结束日);结束日快捷"下个月同日"(5月3日→6月3日)**自动生成**,任何时候可手动改结束日
2. **备注**(预设:暑假 / 学期…可自定义,可不填)
3. **初始收入**:先作为独立收入操作写入流水表(金额 + 账户 + 分类,不限定生活费,可为工资等);可跳过(无初始收入场景,如假期)
4. **资金池**(checkbox 默认开,在初始收入**之后**划分):
   - 资金池大小(默认=初始收入金额;无收入时可从目标账户余额取)
   - 预计保留(默认=大小×30%,可改)
   - 资金池账户(单一,默认=生活费入账账户)
5. **确认预览**:周期/天数/备注/收入/资金池摘要 → 创建

周期结束日到了 → 程序推荐新建下周期(快捷日期);上一周期在封存前持续可修改;封存后才真正结束。

## 6. 封存流程(向导)

> 封存 = 周期真正终结。ended(已到期)≠ sealed(已封存):到期后仍可修改,封存后才只读。

**入口**:应用栏 [封存](当前周期)/ 周期菜单「封存当前周期」;当前周期已 sealed → 按钮置灰。随时可封存;若今天 < 结束日,提示提前封存会把之后日期归入空档,建议先改结束日。

**向导三步**:
1. **检查清单**(不阻断,仅提示)
   - 本周期待归类交易数(有 → 提示去处理)
   - 资金池摘要:剩余 / 可支配;预留项到期提醒;无池周期跳过
   - 长时未校准账户(仅提示)
2. **结余去向**
   - 周期结余 = 收入 − 支出
   - 默认留在原账户(现金自然延续,不产生结转记录);也可选"先转出一部分" → 快捷跳转 记一笔·转账;未记完就封存 = 结余留原账户(不硬卡)
3. **报表归档**:生成报表 checkbox(默认开),PDF 留档 + xlsx 数据,选保存目录

**封存完成** → 立即推荐新建下一周期(快捷日期自动预填:上周期结束日+1,学期/假期模式),可接受或稍后。

**封存后**:
- 只读:该周期内流水不可增删改,周期总览只读样式
- 报表可随时重新导出(不限于封存那一刻)
- **可解除封存(unseal)**:周期管理里解除 → 恢复可修改,报表标记"已过期需重新生成";操作进审计日志
- **跨周期退款**:退款流水记在退款发生日(新周期),refund_of 指回封存周期原交易;封存数据不动

## 7. 导入与对账

- **早期**:AI agent 辅助写入数据库;后续版本做自动化导入程序
- 微信:表头第 17 行;支付宝:GBK、头部 22 行说明 + 表头行
- **去重**:ref_id(微信用交易单号)
- **退款/取消**:status 标签;退款冲减原交易(不当作收入),取消/关闭剔除
- **混合支付**(如 余额宝&支付宝立减):按实际支出拆分到各账户,优惠不输入;拆分明细 group_parent 指向金额最大一笔
- 未命中分类 → 待归类队列

## 8. 报表

> 定位:数据与展示分离——只导出文件、不留内部快照;PDF 留档阅读,xlsx 供再处理。
> 入口:报表 Tab + 周期菜单「生成报表」+ 封存向导自动触发,共用同一生成器。

**生成对话框**:
1. **范围**:周期(下拉,含进行中/已封存)· 自定义日期范围(可跨周期)· **周期对比**(勾选 ≥2 个周期,快捷「与上个周期比」)。周期报表**不含**未归属空档账;自定义范围**含**。
   - **对比模式额外块**:① 总览对比表(指标×周期列+环比变化;周期长短不一 → 日均与占比为主、金额为辅)② 净资产变化(各周期期末净资产 = 当前派生余额 − 期末后流水重建,不需额外快照)。分类结构对比与跨周期趋势即主内容块的"跨周期趋势"图,多选周期自动扩展
   - 文件名:`个人账本_2026暑假对比2025暑假_20260902.pdf`
2. **内容块**(默认全选):
   - 总览:总收入/总支出/结余/**日均**(= 区间总支出 ÷ 区间天数;进行中范围统计含今天)
   - 分类占比:大类饼图 + 表格;**待归类交易单列「未归类」**;**差额调整不计入用途占比**,总览单列"校准/差额调整合计"
   - **跨周期趋势**(统一趋势与对比):表(各周期收入/支出/结余)+ **分类堆叠面积图**,**x 轴 = 按时间序的周期**(不按周/自然月),每周期一列。渲染规则:每根竖条=一个周期,类别从下往上按固定序堆叠;每类的**上边界点**跨周期连成**鲜艳色折线**;相邻两条上边界线之间的区域 = 该类**淡色半透明填充**(0 轴到第一条线为最底类);最顶线 = 该周期总支出。鲜艳线读趋势、淡色带读比例、顶线读总量。单周期报表自动带前后几个周期作上下文(可调);多选周期即对比。可切「金额 / 100% 占比」;周期内节奏由每日收支承担
   - 账户与净资产:各账户余额/类型/本周期变动 + 净资产合计(停用账户灰显不计)
   - 大额 TOP:支出/收入 TOP N(名称/金额/日期/分类)
   - 每日收支:日期/支出/收入/累计
   - 资金池(仅单一周期且带池):预算/已花/剩余/可支配/保留/预留到期提醒
   - 转账汇总:笔数 + transfer 分类小计(互转/充值/提现/理财结算/存取)
3. **输出**:PDF + xlsx 勾选;保存目录;文件名 `个人账本_2026暑假_20260902.pdf`

**报表 Tab**:历史报表列表(范围/时间/路径)+ 打开 / 重新生成 / 删除;重新生成走同一生成器(封存后改口径、跨周期退款补账后可随时出新版)。

**导出 CSV**:全表导出(不只流水表),菜单·文件。

## 9. 分类管理与待归类队列

**入口**:导航 Tab「分类」(内容视图)+ 设置菜单「分类管理 / 待归类队列」(定位到对应区块)。

**分类 Tab 布局**:
- 左:两列表(支出大类 / 收入大类),行 = 颜色块 + 名称 + 近期占比;**拖排 = sort_order**,直接决定堆叠面积图自下而上叠放序
- 右:选中分类详情——名称、主题色(调色板,已占用标注)、**关键词**(导入自动归类)、使用统计(本周期金额/占比)
- 顶部:[＋ 新建分类] [重命名] [改色] [合并] [删除]

**规则**:
- 一期平铺大类(小类 = 备注,不另建类);`parent_id` 预留
- 新建:填名称 → 自动分配调色板下一个未占用色(可改)
- **保留分类**:「其他」可删可重建;「差额调整」「未归类」系统保留、不可删
- **删除分类**:必须先处理其下交易(改为其他 / 移到指定类 / 回待归类队列),不静默
- **合并分类**:B 并入 A → 交易改 A、关键词/颜色并入(B 色丢弃),B 删除
- **用水**:记录界面独立入口,落库归「居住」,备注记"用水"

**待归类队列**:
- 来源:导入流水交易名命中 关键词 与 平台分类映射 均失败 → 进队列;手记一般直接选类、不进队列
- 界面:每行 = 日期/名称/金额/平台分类/**建议分类(下拉)**,✓= 建议已命中
  - 分配分类(关键词命中高亮、平台分类映射预填)
  - 忽略(保持未归类,报表单列「未归类」灰色)
  - 批量:多选行一起分配;同平台分类一键批量
  - 处理中发现新类 → 直接新建
  - 行尾按钮:把"平台分类→本地分类"存为映射,以后自动
- 不强制清零;待归类计数显示在分类/流水 Tab,封存向导检查清单提示

## 10. 数据与安全

- SQLite 单文件(SQLCipher 全库加密,打开需密码),存用户目录
- 备份:一键导出 DB + 全量 CSV

## 11. 迁移(二期)

- 手工账本(账单.xlsx):日期序列号归一化、公式文本求值(白名单)、旧分类映射、转账反推,流水带 legacy_ref 审计
- 微信/支付宝导入
- 账户初始余额来源待定(建行卡 2026-06-11 后流水缺失)

---

## 待定 / 待讨论

- [ ] 退款无法匹配原交易时如何处理
- [ ] 迁移范围与账户初始余额
- [ ] 对账流程细节(差异如何呈现、是否自动补记)
- [ ] UI 布局与交互细节(进行中)

## 已定备注(本轮)

- 资金池:退款恢复池;**收入一律不计入池**;**转账出池账户可选"计入池"**(默认不计);**转入池账户、其他账户间转账不计入池**;**多池设计保留,一期只做单一池**
- **月历**:周期内日期格不同背景高亮,周期外淡色;点某天查看当天;月历左右翻动**不影响**右侧周期视图(Outlook 式独立导航)
- **顶栏**:显示 剩余 与 可支配 并列
- **记账**:以"笔"为单位**批量录入**;**支出/收入/转账三模式切换,每模式是一张对齐列表**(支出列=日期/名称/类别/渠道/金额/账户/备注/**不入池勾选**;收入列去渠道;转账列=转出/转入/本金/实际到账/类别/备注/计入池勾选);金额元,Decimal 2位;每行日期可独立选
- **名称(name)**:交易名独立字段,与备注分开
- **储值卡**:公交卡=模型一(充值即支出,不建账户);水卡=模型二(充值转账+用卡支出,prepaid 账户计净资产);充值占用池额度
- **无周期时间段**:账永远可记,周期只管归类;未归属账目 period_id=null,灰色徽标,流水 tab 可筛选"未归属";周期新建/改范围/删除时自动重挂;月历不受影响;周期报表不计空档账,自定义范围报表计;封存后不可塞账,空档可补建周期
- **无池周期**(假期等):总览隐藏资金池区块(预算/可支配/保留线/提醒),记一笔隐藏"不入池/计入池"列;**池可中途补建**(周期总览"设置资金池"),已花由账目派生、补建后自动正确;封存跳过池检查
- **UI 计划新增**:周期管理**时间线视图**(周期条 + 空档高亮 + 一键补建周期)
- **顶部三层结构**:
  - 菜单栏(按领域):
    - `文件`:新建/打开/最近账本 · 导入(微信/支付宝)· 导出(全量CSV/备份db)· 退出
    - `周期`:新建周期(向导)· 封存当前周期 · 周期管理(时间线视图)
    - `账户`:新建账户 · 账户管理 · 校准余额
    - `设置`:分类管理与待归类队列 · 密码与加密 · 偏好
    - `关于`:版本/数据位置/说明
  - 应用栏:账本名 · 周期切换 ◀▶ · 剩余+可支配 · [＋记一笔] [封存]
  - 导航 Tab:总览 | 流水 | 分类 | 账户 | 报表(内容视图)
  - **原则**:菜单 = 动作入口(弹对话框),Tab = 内容视图;账户/周期两类动作可同时从菜单与应用栏进入,不冲突
- **校准余额**:校准=一次对账动作;三种处理——记调整流水(差额分类固定「差额调整」、source=calibration、in_pool=false,方向按实际>账面自动推导)/ 补记真实明细 / 仅更新基准;每次校准留审计记录(时间/账面/实际/差额/方式/备注)
- **封存流程**(见 §6):向导三步(检查清单→结余去向→报表归档);封存后才只读;封存完成推荐新建下周期;**可解除封存(unseal)**,报表标记过期需重生成;跨周期退款靠 refund_of 指回封存周期原交易,封存数据不动
- **凌晨补记**:封存是手动动作、不自动触发;ended 周期仍可改 → 00:30 补记昨日账 = 改行内日期即可。批量列表**新行日期默认沿用上一行**。封存向导确认语提醒"封存后不可再补记",不硬性阻断
- **偏好设置开关「凌晨宽限记账」**:开(默认)时,记一笔默认日期 = 若当前时间 < 2:00 且今天尚无记录 → 昨天,否则今天;关 = 始终今天。可改宽限时刻(默认 2:00)
- **报表生成器**(见 §8):范围=周期/自定义(自定义含空档账);内容块默认全选;日均口径 = 区间支出÷区间天数,进行中范围统计含今天;**差额调整不计用途占比、总览单列合计**;待归类单列「未归类」;报表栈 = QuestPDF + ClosedXML + ScottPlot 导出图
- **趋势/对比图统一 = 跨周期堆叠面积图**:x 轴 = 按时间序的周期(不按周/自然月),每周期一列;每类上边界跨周期连成鲜艳折线,相邻边界间 = 淡色半透明填充,顶线 = 周期总支出。趋势读线、比例读带、总量读顶线;可切「金额 / 100% 占比」;同类别同色。单周期报表自动带前后几周期作上下文;多选周期 = 对比。周期内节奏走每日收支表
- **类别主题色全局一致**:颜色存于 categories.color,所有图表(饼/堆叠面积/占比/流水标签)统一引用;新建类别默认从固定高区分度调色板按序自动分配(色盲友好),可手动改;「未归类」固定灰色、「差额调整」专属保留色;收入分类同机制。类别 sort_order 决定堆叠面积图自下而上叠放序(分类管理拖排)
- **周期对比**:范围=周期/自定义/**对比(勾选≥2,快捷「与上个周期比」)**;对比块=总览对比表(日均与占比为主)+ 净资产变化(期末净资产=当前派生余额−期末后流水重建);分类结构对比即主图(跨周期堆叠面积)多选周期。文件名 `<账本>_<周期A>对比<周期B>_<日期>`
- **分类管理与待归类队列**(见 §9):分类 Tab = 左列表(支出/收入大类,拖排即叠放序)+ 右详情(颜色/关键词/统计);新建自动配色、可改;「差额调整/未归类」保留不可删;删除必须先处理其下交易;合并分类并入关键词颜色。待归类队列 = 导入未命中关键词与平台映射的交易,逐条/批量分配或忽略,可存"平台分类→本地分类"映射,不强制清零
- **账户视图(账户 Tab,全局,不受周期切换影响)**:顶 = 净资产合计 + 本周期变动 + [＋新建账户][校准余额][刷新];账户列表按平台分组可折叠(微信/支付宝/银行/投资/储值卡…),行 = 余额 + 本周期变动 + 类型,固定存款标到期日;点行 → 下方面板(余额构成=基准+收/支/转、本周期收/支/转、该账户流水、[编辑][校准]);停用灰显不计净资产;可选:净资产按现金类/理财类/储值卡小拆
- **流水 Tab(内容视图)**:顶部筛选栏(周期[本周期/全部/未归属]·方向·账户·分类·状态[正常/已退款/已取消/含调整]·日期区间·关键词·重置)。列表**按日分组**(组头=星期+当日支出/收入+[补记],组可折叠);列 = 名称(徽标:已退款/已取消/未归属⚠/混合拆分↩/校准ⓘ)·类别(彩色块,差额调整/未归类保留样式)·账户(转账显示"A→B")·金额(右对齐,支出红−/收入绿+,转账=本金+Δ徽标)·渠道·池(入池实心/不入池空心)·备注(悬停全文)·操作(悬停:编辑/标记退款/标记取消/入池切换/删除;双击=编辑)。未归属灰显;混合拆分子行缩进指主笔;底部=当前筛选支出/收入/笔数;按日期倒序、惰性加载不翻页;列可隐藏/重排(可选)
- **账本文件与启动**:账本 = 单个加密 SQLite 文件,后缀 **`.lbook`**(避开 `.db` 通用冲突与 Calibre 的 `.pbook` 插件;检索无主流占用)。**启动默认自动加载上次账本**,口令按开关走 Windows 凭据管理器或每次输入;**双击 `.lbook` 打开**:首次运行注册 HKCU 文件关联(免管理员),文件路径经命令行参数传入。偏好存明文 `%APPDATA%\账单管理\app.json`(账本外,含上次账本路径/记住口令开关/凌晨宽限等);口令本身不入该文件
