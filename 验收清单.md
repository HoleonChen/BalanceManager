# 验收清单(2026-09-02 批次)

Windows 上 `dotnet build ZhangDan.sln -c Debug && dotnet run --project src/账单管理` 后逐项点检。

## 0. 入口自检
- [ ] 工具 → 数据自检:弹窗「数据自检通过」,列出全部通过项(建库写读 / 无周期未归属 / 补建回填 / 自动归属 / 跨期未归属 / 当日流水+合计 / 转账字段与不计入 / 作废撤出 / 就地编辑 / 范围合计 / 错口令拦截 / 重开读回)。

## 1. 账本与启动
- [ ] 文件 → 新建账本:口令 ≥6 位;路径默认到「文档\账单管理」并补 .lbook;同名已存在会被拦。
- [ ] 关闭账本 → 主区回到提示页;退出重开 → 自动弹出上次账本口令框。
- [ ] 口令错 → 提示「口令错误…遗忘无法找回」;对 → 打开。

## 2. 周期与账户
- [ ] 文件 → 新建记账周期:默认名「生活费」、起=今天、止=今天+30;结束后顶栏蓝字显示「周期:生活费(…~…)」。
- [ ] 工具 → 账户管理 → 新建账户(可先建 2 个:如「微信零钱」钱包/微信、「建行卡」银行卡/银行),列表出现;入账余额可空。
- [ ] 账户不足 2 个时打开「记转账」应给出内联引导。

## 3. 记账(支出/收入)
- [ ] ＋ 记一笔:切收入/支出分类联动;金额留空/非数字/≤0 报错;名称必填。
- [ ] 账户行「新建账户…」:内联建账户并自动选中。
- [ ] 勾「记到昨天」:凌晨宽限开关在 工具→设置 里可关;编辑流水时该勾选被锁为固定日期。
- [ ] 保存后主区跳到该笔所在日期,列表出现;金额支出红「-」、收入绿「+」。
- [ ] 顶栏 ◀▶ / 「今天」可翻日期;日期标注带「今天/昨天/明天」。

## 4. 转账
- [ ] ⇄ 转账:转出/转入不能同一账户;本金>0;「实际到账」留空=本金(Δ0);填 10000/10062.5 → 显示「浮动 Δ = +¥62.50」。
- [ ] 保存后当日流水显示「A → B」、金额附 (Δ+/-…);当日支出/收入合计不含转账。
- [ ] 类别五选一(互转/充值/提现/理财结算/存取)。

## 5. 编辑与作废
- [ ] 双击某笔支出/收入 → 编辑流水(分类/金额/名称等可改;日期固定)保存后合计刷新。
- [ ] 双击转账 → 编辑转账(转出/转入/本金/实际到账→Δ/类别;日期固定)保存后列表与 Δ 刷新。
- [ ] 右键某笔 → 作废/删除:确认后从列表与合计撤出;重开账本仍不在(软删留库)。

## 6. 周期流水
- [ ] 工具 → 查看本周期流水:列出一周期全部流水(含转账 A→B)与周期支出/收入合计、笔数;未建周期时给引导提示。

## 7. 数据归属抽查(如需)
- [ ] 先记几笔、再补建覆盖它们的周期 → 顶栏周期 chip 出现;工具 → 查看本周期流水里能看到先前那几笔(补建回填)。

## 数据与迁移
- 账本 `.lbook` 全库 SQLCipher 加密;偏好明文在 `%APPDATA%\账单管理\app.json`(不含口令)。
- schema v2 自动迁移:老 v1 测试库打开即补 `transfer_kind` 列,无需手工处理。
