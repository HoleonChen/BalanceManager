<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- mac 上跨平台编译检查需要 -->
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>

    <RootNamespace>ZhangDan.App</RootNamespace>
    <AssemblyName>ZhangDan.App</AssemblyName>
    <Version>0.1.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\ZhangDan.Core\ZhangDan.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Fluent 风格控件库(Windows 11 观感) -->
    <PackageReference Include="WPF-UI" Version="3.0.5" />
    <!-- 报表:PDF(QuestPDF Community)/Excel(ClosedXML)/图表(ScottPlot 出 PNG) -->
    <PackageReference Include="QuestPDF" Version="2025.12.4" />
    <PackageReference Include="ClosedXML" Version="0.105.1" />
    <PackageReference Include="ScottPlot" Version="5.0.56" />
  </ItemGroup>

</Project>
